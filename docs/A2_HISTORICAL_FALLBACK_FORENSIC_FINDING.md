# A2 — Historical Fallback Geometry Fingerprint

Status: **Forensic evidence / not production authority**  
Date: **2026-09-17**

## Finding

The retained exact-head historical-builder lineage artifact from workflow run `35267020164` contains an Amisos result at checkpoint:

```text
e67cdd3f7a3b89631c497f0d2ec1ac9d24444364
```

The result is:

```text
vertexCount = 6
area        = 0.00001040999984525115
```

with polygon:

```text
[
  [35.96245, 41.13693],
  [35.96145, 41.13866],
  [35.95945, 41.13866],
  [35.95845, 41.13693],
  [35.95945, 41.13519],
  [35.96145, 41.13519]
]
```

## Geometric fingerprint

This polygon is a regular six-vertex fallback hexagon with an effective radius of approximately `0.002` coordinate units.

For a regular hexagon generated as:

```text
x = cx + cos(angle) * r
y = cy + sin(angle) * r
```

its theoretical area is:

```text
(3 * sqrt(3) / 2) * r^2
```

For `r = 0.002`, this is approximately:

```text
1.0392304845e-5
```

The retained polygon area is `1.040999984525115e-5`, which is consistent with the documented historical six-vertex fallback construction after coordinate serialization/rounding. The vertex spacing and six-fold structure independently identify this as fallback-family geometry rather than a normal five-vertex Voronoi cell.

## New candidate-center lineage finding

The fallback fingerprint can now be traced one level deeper: the observed center is not arbitrary and is not the Amisos metadata centroid itself.

At the historical builder lineage used by the fallback path, Amisos metadata carries centroid:

```text
[36.33, 41.29]
```

The fallback resolver constructs candidate centers from the historical centroid using deterministic radial search. The retained `e67cdd3f...` fallback center is approximately:

```text
[35.96045, 41.13693]
```

The displacement from `[36.33, 41.29]` is:

```text
dx ≈ -0.36955
dy ≈ -0.15307
radius ≈ 0.400000
```

This is an exact fingerprint of the fallback search grid:

```text
searchPass = { radialStep: 0.005, maxRadius: 0.75, directions: 32 }
radius     = 0.400000
 direction = 18
angle      = 18 / 32 * 2π = 202.5°
```

Using the historical centroid and that deterministic search coordinate gives:

```text
36.33 + cos(202.5°) * 0.4 ≈ 35.960448186995485
41.29 + sin(202.5°) * 0.4 ≈ 41.13692662705396
```

which matches the retained fallback center after the observed coordinate serialization/rounding.

This is important because it proves that the `e67cdd3f...` geometry is not simply a `0.002` hexagon around the original Amisos city anchor. It is a `0.002` hexagon around the **first accepted physical-land candidate generated by a deterministic radial reconciliation search**.

The historical implementation explicitly uses candidate centers, searches radial rings from the original seeds, and then feeds accepted centers into the six-vertex fallback builder. Commit `58f20d8f7b85f6dae61521a3509eb569d022bf8b` also records the intended behavior: the search must continue from the original historical centroid even when that seed itself is not physical land. fileciteturn456file0L3-L11

The historical diagnostic commit further records the radial-search construction and candidate provenance, including the `0.005 / 0.75 / 32` search pass. fileciteturn462file0L3-L7

## Historical code correlation

Commit `0dd1dadb90103b5706fd87c470b8d4a6dc85f496` introduced `createAnchorFallbackPolygon()` and generated six-vertex fallback polygons. fileciteturn447file0L3-L11

Later history changed the fallback acceptance path so that the tiny fallback could use the Anatolia envelope rather than requiring the centroid itself to be physical land; commit `38ecea26143b69c04e4411c0bbd7a7071333538b` explicitly changed that guard and retained a `0.002` six-vertex construction. fileciteturn448file0L3-L11

The later historical fallback resolver contains the following deterministic structure:

```text
historical centroid
      ↓
radial search pass
      ↓
accepted physical-land candidate center
      ↓
regular six-vertex polygon
      ↓
round/serialize
```

Therefore the `e67cdd3f...` geometry is not merely “a small polygon”: its exact six-vertex structure, area, and center displacement provide a direct numerical fingerprint of the historical fallback family and its candidate-center search behavior.

## What this proves

It proves that the historical builder lineage can enter a fallback representation whose area is approximately `1.04e-5`, and that this representation was actually observed for the `pontus-amisos` forensic target at checkpoint `e67cdd3f...`.

It now also proves that the observed fallback center is generated by the historical deterministic radial candidate search from the Amisos centroid, specifically the `r=0.4`, `direction=18` point of the `0.005/0.75/32` search pass.

It does **not** yet prove that this fallback generated the original `2.27e-13` observation.

The original target is approximately:

```text
2.27e-13
```

For the same regular-hexagon construction, that area would correspond to an effective radius of approximately:

```text
sqrt(2.27e-13 / (3*sqrt(3)/2)) ≈ 2.96e-7
```

That is roughly four orders of magnitude smaller than the `0.002` fallback radius fingerprint observed at `e67cdd3f...`.

This makes the next question more precise: **did a later fallback/serialization stage reduce an already-fallback polygon toward an effective radius near `3e-7`, or did the original `2.27e-13` artifact come from a different degenerate representation?**

## Canonical comparison

The same forensic lineage artifact records canonical `6b7424125eee4a1c72925b7a1780c68e695e9ba3` Amisos geometry as:

```text
vertexCount = 5
area        = 0.0067547530500178254
```

Thus the historical `e67cdd3f...` result is not a small numerical perturbation of the canonical polygon. It is a different geometric representation and a different spatial footprint.

## Locked interpretation

```text
e67 checkpoint
    ↓
Amisos historical centroid [36.33, 41.29]
    ↓
radial search: r=0.4, direction=18, pass 0.005/0.75/32
    ↓
accepted physical-land candidate ≈ [35.96045, 41.13693]
    ↓
6-vertex fallback, r≈0.002
    ↓
area ≈ 1.04e-5
```

This is now a **confirmed historical fallback-family observation with candidate-center provenance**.

The A2 root cause remains **PROVENANCE / HISTORICAL ARTIFACT DISCONTINUITY** until the `2.27e-13` artifact itself is reproduced or its exact serialized representation is identified.

No production geometry, `MIN_AREA`, MapBin, GPU, or runtime behavior is changed by this finding.

## Next forensic action

The next trace should follow the exact historical fallback candidate into every downstream representation and compare the first point at which its geometry changes:

- accepted candidate center,
- six-vertex construction,
- physical-land validation,
- coordinate rounding,
- polygon deduplication,
- asset JSON serialization,
- any import/export transformation,
- binary conversion, and
- area calculation.

In parallel, the historical search history should be checked for any later change to the `0.002/0.001/...` polygon radii or any alternate producer that could create an effective radius near `2.96e-7`.

The acceptance target remains an exact numerical chain ending at `~2.27e-13`, not merely another small fallback polygon.
