import assert from "node:assert/strict";
import { createTerrainTopology } from "./TerrainTopologyDispatcher.js";
import { validateTerrainIndexTopology } from "./TerrainTopologyValidator.js";
import { validateTerrainExactCellCoverage } from "./TerrainTopologyExactCoverageValidator.js";
const size=5;const positions=new Float32Array(Array.from({length:size*size},(_,i)=>[i%size,Math.floor(i/size)]).flat());
function area(indices){let total=0;for(let i=0;i<indices.length;i+=3){const p=[indices[i],indices[i+1],indices[i+2]].map(v=>[v%size,Math.floor(v/size)]);total+=Math.abs((p[1][0]-p[0][0])*(p[2][1]-p[0][1])-(p[1][1]-p[0][1])*(p[2][0]-p[0][0]))/2;}return total;}
for(const [variant,edges] of [["base",{}],["edge-stitch-2to1",{north:"neighbor-coarser"}],["corner-ne",{north:"neighbor-coarser",east:"neighbor-coarser"}],["corner-se",{east:"neighbor-coarser",south:"neighbor-coarser"}],["corner-sw",{south:"neighbor-coarser",west:"neighbor-coarser"}],["corner-nw",{west:"neighbor-coarser",north:"neighbor-coarser"}]]){const topology=createTerrainTopology({size,edges,variant});assert.equal(topology.variant,variant);assert.ok(topology.indices instanceof Uint32Array);assert.ok(topology.indexCount>0);validateTerrainIndexTopology({indices:topology.indices,vertexCount:topology.vertexCount,positions});assert.equal(area(topology.indices),16,`${variant} area`);const exact=validateTerrainExactCellCoverage({indices:topology.indices,positions,size});assert.equal(exact.completeExactCoverage,true,`${variant} exact coverage`);assert.equal(exact.uncoveredArea,0,`${variant} uncovered area`);assert.equal(exact.overlapArea,0,`${variant} overlap area`);}
assert.throws(()=>createTerrainTopology({size,variant:"base",edges:{north:"neighbor-coarser"}}),/Base topology cannot encode/);assert.throws(()=>createTerrainTopology({size,variant:"edge-stitch-2to1",edges:{north:"neighbor-coarser",east:"neighbor-coarser"}}),/exactly one/);assert.throws(()=>createTerrainTopology({size,variant:"corner-ne",edges:{north:"neighbor-coarser",east:"neighbor-finer"}}),/requires neighbor-coarser/);assert.throws(()=>createTerrainTopology({size,variant:"unknown"}),/Unknown terrain topology variant/);console.log("Phase E terrain topology dispatcher exact coverage: PASS");
