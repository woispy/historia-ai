import { createTerrainEdgeIndexTopology } from "./TerrainEdgeIndexTopology.js";
const VARIANTS=Object.freeze(["base","edge-stitch-2to1","corner-ne","corner-se","corner-sw","corner-nw"]);
export function createTerrainTopology({size,edges={},variant="base"}={}){if(!VARIANTS.includes(variant))throw new Error(`Unknown terrain topology variant: ${variant}.`);const resolved={north:edges.north??"same",east:edges.east??"same",south:edges.south??"same",west:edges.west??"same"};if(variant==="base")for(const edge of Object.keys(resolved))if(resolved[edge]!=="same"&&resolved[edge]!=="boundary")throw new Error(`Base topology cannot encode transition on ${edge}.`);if(variant.startsWith("corner-")){const pair={"corner-ne":["north","east"],"corner-se":["east","south"],"corner-sw":["south","west"],"corner-nw":["west","north"]}[variant];for(const edge of pair)if(resolved[edge]!=="neighbor-coarser")throw new Error(`${variant} requires neighbor-coarser transitions on ${pair.join(" and ")}.`);}if(variant==="edge-stitch-2to1"&&Object.values(resolved).filter(v=>v==="neighbor-coarser").length!==1)throw new Error("edge-stitch-2to1 requires exactly one coarser neighbor edge.");const topology=createTerrainEdgeIndexTopology({size,edges:resolved});return Object.freeze({...topology,variant});}
export { VARIANTS };
