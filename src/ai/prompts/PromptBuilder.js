/**
 * ============================================================================
 * Prompt Builder
 * ============================================================================
 *
 * Base builder used by every prompt.
 */

export function createPrompt(text) {
  return text.trim();
}