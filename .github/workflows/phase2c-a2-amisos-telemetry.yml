name: Phase 2.8-C A2 Amisos Telemetry

on:
  pull_request:
    branches:
      - integration/phase-a-h-production
  workflow_dispatch:

env:
  A2_V15_SOURCE_SHA: 3575c1bccf94a322fed175958ce786531b142497

jobs:
  a2-amisos-telemetry:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm install --include=dev --no-audit --no-fund --package-lock=false

      - name: Generate required GIS runtime assets
        run: npm run build:assets

      - name: Fetch retained V15 forensic source
        shell: bash
        run: |
          git fetch --no-tags --depth=1 origin "$A2_V15_SOURCE_SHA"
          V15_SOURCE_PATH="$(git ls-tree -r --name-only "$A2_V15_SOURCE_SHA" | grep -E '(^|/)AnatoliaPhase2DGeometryBuilder\.js$' | head -n 1)"
          test -n "$V15_SOURCE_PATH"
          git show "$A2_V15_SOURCE_SHA:$V15_SOURCE_PATH" > "$RUNNER_TEMP/AnatoliaPhase2DGeometryBuilderV15.mjs"
          echo "A2_V15_SOURCE_FILE=$RUNNER_TEMP/AnatoliaPhase2DGeometryBuilderV15.mjs" >> "$GITHUB_ENV"
          echo "A2_V15_SOURCE_PATH=$V15_SOURCE_PATH" >> "$GITHUB_ENV"
          echo "Resolved V15 source: $V15_SOURCE_PATH"

      - name: Run isolated A2 Amisos edge telemetry
        run: node tools/tests/phase2d-a2-amisos-edge-telemetry.mjs

      - name: Upload A2 edge telemetry
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase2.8-c-a2-amisos-edge-telemetry
          path: artifacts/phase2.8-c/a2-amisos-edge-telemetry.json
          if-no-files-found: error
