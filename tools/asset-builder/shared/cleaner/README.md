# Output Directory Cleaner

Removes previously generated assets before the Asset Builder writes new files.

This guarantees deterministic builds.

Responsibilities

- Remove generated assets
- Preserve directory structure
- Execute before every build